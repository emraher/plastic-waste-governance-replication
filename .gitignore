# Third-party inputs are not redistributed. See data/README.md for how to
# obtain them; the tables the authors compiled are tracked and travel with the code.
data/baci/
data/comtrade/
data/corruption/
data/controls/
data/gravity/

# Regenerated by the pipeline. The two shipped artifacts under output/ are
# force-added below; everything else there is a build product.
output/*
!output/plastic_corruption_panel.parquet
!output/plastic_corruption_panel.csv.gz
!output/product_class_codes.csv

# Stata cross-check writes this from the panel
stata/*.dta
stata/*.log

.Rhistory
.RData
.Rproj.user/
.DS_Store
